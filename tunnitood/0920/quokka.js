let array = [1, 2, 3, 4, 47]
let b = array[4]
console.log(b)
/*
const deconstruct = () => {
  const [b, , c, , a] = array()
  return { a, b, c }
}
console.log(deconstruct())
*/


;(() => {   fn(...).callback is not a function
    const functionCaller = fn => {
        if (!fn().callback)
}})

const raimo = {
  name: "Raimo",
  school: "TLU",
  ":DD :DD": "Koolon dee"
}
console.log(raimo.name + " " + raimo.school)
console.log(raimo["name"] + " " + raimo["school"])
console.log(raimo[":DD :DD"])
